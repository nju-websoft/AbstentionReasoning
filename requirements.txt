# Python 3.10 is recommended.
#
# Core pipeline dependencies.
numpy==2.2.6
torch==2.7.0
tqdm==4.67.1
transformers==4.52.4
vllm==0.9.2

# Optional: only needed when using --answer_judge ollama in Stage 3.
ollama==0.5.1
